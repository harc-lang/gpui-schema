[package]
name = "gpui-schema"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
gpui = "0.2"
gpui-component = "0.5"
schemars = { version = "1", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[example]]
name = "demo"
